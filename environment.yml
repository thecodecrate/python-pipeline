name: venv
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.13
  - pip
  - pip:
    # jupyter
    - ipykernel
    - debugpy
    # test suite: pytest
    - pytest
    - pytest-asyncio
    - pytest-cov
    - pytest-describe
    # tool for updating semantic versioning over multiple files
    - bumpver
    # tools for building package for PyPI
    - build
    - twine
